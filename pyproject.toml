[project]
name = "microrelief"
version = "0.4.0"
description = "Microrelief from raw airborne LiDAR: DTM, DSM and CHM that declare what each cell is made of"
requires-python = ">=3.12"
dependencies = [
    "laspy[lazrs]>=2.5",
    "numpy>=1.26",
    "scipy>=1.11",
    "rasterio>=1.3",
    "pyproj>=3.6",
]

[project.optional-dependencies]
site = ["matplotlib>=3.8"]
# The DGT catalogue client is the only thing here that touches the network. The offline
# core — reading a LAZ, computing a basis, writing a record — must not have to carry an
# HTTP client to be installed.
dgt = ["requests>=2.31"]
# `requests` is repeated here on purpose: CI runs `uv sync --extra dev --extra site`, so
# without it the provider tests would be uninstallable in CI while passing locally.
dev = ["pytest>=8.0", "ruff>=0.5", "mypy>=1.10", "requests>=2.31"]

[project.scripts]
microrelief = "microrelief.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src"]

[[tool.mypy.overrides]]
module = "affine.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "laspy.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "scipy.*"
ignore_missing_imports = true

# rasterio ships no py.typed marker, so strict mode refuses the import outright.
[[tool.mypy.overrides]]
module = "rasterio.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.sdist]
# The sdist is the package, not the piece: the viewer's PNGs, the judge rounds, the sample and
# the case-study docs live in the repository and in the Release, not in every `pip download`.
# The tests go with them: they read viewer/provenance.json, docs/live-smoke.md, aoi/ and the git
# index, so a tests/ directory shipped without those would be a suite that cannot run. The
# repository is where the tests run (README §Install). Measured 2026-08-26: 35.6 MB -> 163 KB.
exclude = [
    "/viewer", "/docs", "/examples", "/styles", "/skills", "/outputs*", "/aoi", "/scripts", "/tests",
    "/.github", "/.impeccable",
]
